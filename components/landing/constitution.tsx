import Link from "next/link";
import styles from './landing.module.css';

export default function Constitution() {
    return (
        <Link href="" target="_blank" rel="noopener noreferrer" className={styles.links}>
            Constitution
        </Link>
    );
}
