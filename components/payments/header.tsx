import styles from './payments.module.css';

export default function PaymentsHeader() {
    return (
        <div className={styles.header}>
            <div className={styles.title}>Re Up | Payments</div>
        </div>
    );
}
