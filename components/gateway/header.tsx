// components/GatewayHeader.tsx
import styles from './gateway.module.css';

export default function GatewayHeader() {
    return (
        <div className={styles.header}>
            <div className={styles.title}>Re Up | Gateway</div>
        </div>
    );
}
